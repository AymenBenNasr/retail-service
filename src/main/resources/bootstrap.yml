spring:
    config:
     import: optional:configserver:http://localhost:8071 
    application:
     name: retail-service 
    profiles:
      active: dev
    cloud:
        config: 
            uri: http://configserver:8071